

``` sh
# first add individual json files to notes folder like {title_name}_0_1, {title_name}_1_2, etc
# add credentials to user.json
# add relay(s) you care to publish to in upload.ts
npm install
npx esrun src/upload.ts
```

`
